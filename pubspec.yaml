name: rarUI
description: "RarUI"
version: 0.0.1
homepage: ""
repository: ""
issue_tracker: ""

environment:
  sdk: ^3.6.0

dependency_resolution:
  resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  br_validators: ^2.0.4
  qr_code_scanner_plus: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 5.0.0
  
flutter:
  uses-material-design: true
