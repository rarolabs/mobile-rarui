image: ghcr.io/cirruslabs/flutter:latest

stages:
- test

test_job:
  stage: test
  script:
    - echo "Rodando testes na versão $FLUTTER_VERSION"
    - flutter test
  rules:
    - if: $CI_PIPELINE_SOURCE == 'push'
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_MERGE_REQUEST_TARGET_BRANCH_NAME =~ /^(develop|candidate)$/
      when: always
    - when: never

  



