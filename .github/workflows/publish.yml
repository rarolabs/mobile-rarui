name: Publish to pub.dev
 
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Tag Git e versão do package a ser publicada (ex: 1.2.3)"
        required: true
        type: string
 
      changelog:
        description: "CHANGELOG.md notes"
        required: true
        type: string
        default: "- Correções de bugs e melhorias de desempenho."
 
 
jobs:
  validate:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
 
    steps:
      - uses: actions/checkout@v4
 
      - name: Validate version do pubspec.yaml e CHANGELOG.md
        run: |
          VERSION="${{ github.event.inputs.version }}"
 
          echo "🔍 Validando versão $VERSION..."
 
          if grep -q "^version: $VERSION" pubspec.yaml; then
            echo "::error::A versão $VERSION já está no pubspec.yaml."
            exit 1
          fi
 
          if grep -q "^## $VERSION" CHANGELOG.md; then
            echo "::error::A versão $VERSION já existe no CHANGELOG.md."
            exit 1
          fi
 
          echo "✅ Versão não encontrada, continuar."
 
  update_version:
    needs: validate      # só executa se validar passou
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
 
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
 
      - name: Atualizar versão no pubspec.yaml
        run: |
          sed -i "s/^version:.*/version: ${{ github.event.inputs.version }}/" pubspec.yaml
 
      - name: Atualizar CHANGELOG.md
        run: printf '%s\n\n%s\n\n%s\n' "## ${{ github.event.inputs.version }}" "${{ github.event.inputs.changelog }}" "$(cat CHANGELOG.md)" > CHANGELOG.md
 
      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
 
          git add pubspec.yaml CHANGELOG.md
          git commit -m "chore: release ${{ github.event.inputs.version }}" || echo "Nenhuma mudança"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
 
  publish:
    needs: update_version
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
 
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
 
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.32.2"
 
      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test
      - run: dart pub publish --dry-run
 
      - name: Save pub.dev credentials
        run: |
          mkdir -p ~/.config/dart
          printf '%s' "${{ secrets.CREDENTIAL_JSON }}" > ~/.config/dart/pub-credentials.json
 
      - name: Publish package
        id: publish
        run: dart pub publish --force
 
      - name: Cleanup credentials
        if: always()
        run: rm -rf ~/.config/dart
 
      - name: Criar e push da TAG após sucesso
        if: success()
        run: |
          git tag ${{ github.event.inputs.version }}
          git push origin ${{ github.event.inputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}